# Install progressively, one phase at a time — you don't need everything on day 1.
# Pins are loose on purpose; run `pip-compile` if you want a lockfile.

# --- Phase 1: RAG core -------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sentence-transformers>=3.0        # local embeddings (pulls torch)
faiss-cpu>=1.8                    # local vector store
openai>=1.40                      # client works for Ollama + any OpenAI-compatible API

# --- Phase 2: agent ----------------------------------------------------------
langgraph>=0.2
langchain-core>=0.3

# --- Phase 3: MCP ------------------------------------------------------------
mcp>=1.2                          # official Model Context Protocol SDK (FastMCP)

# --- Phase 4: voice (uncomment when you get here) ----------------------------
# faster-whisper>=1.0
# livekit-agents>=0.11
# kokoro-onnx>=0.3                # or: piper-tts

# --- Phase 5: eval + monitoring ----------------------------------------------
mlflow>=2.14
# ragas>=0.2                      # richer RAG metrics
# evidently>=0.4                  # drift dashboards
# langfuse>=2.0                   # step-level tracing

# --- dev ---------------------------------------------------------------------
pytest>=8.0
