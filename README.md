# AgenticVoiceRAG
A conversational agent that a user can talk to, which answers questions over a private knowledge base, can take actions through tools you expose via an MCP server, and is deployed as monitored microservices. Pick a domain you find interesting so you can speak to it in interviews.
